<Window x:Class="Lab3.AddWorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add" Height="200" Width="400">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Name:</Label>
        <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" Margin="4" />

        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Work:</Label>
        <TextBox x:Name="WorkBox" Grid.Row="1" Grid.Column="1" Margin="4" />

        <Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">Date (yyyy-MM-dd):</Label>
        <TextBox x:Name="DateBox" Grid.Row="2" Grid.Column="1" Margin="4" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Width="80" Margin="0,0,8,0" Click="Ok_Click" IsDefault="True">OK</Button>
            <Button Width="80" Click="Cancel_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>